@using Sandbox;
@using Sandbox.UI;
@inherits PanelComponent
@namespace Sandbox

<root>
	<WebPanel Url="@Url" AcceptsFocus=@false class="WebPanel" />
</root>

@code
{
    [Property]
    public string Url { get; set; } = "https://infinitemac.org/embed?disk=System+1.0&infinite_hd=true&machine=Mac+128K&screen_scale";

    public void OnNetworkSpawn(Connection _) => Network.AssignOwnership(Rpc.Caller);
}
